# Zelretch Addons — pip requirements
# Install with: pip install -r addons.txt
#
# Each plugin should declare its own optional dependencies. The loader will
# skip a plugin gracefully if its imports fail.

# Core utilities
emoji
fonttools
pillow
pyfiglet
pygments
pyjokes
wikipedia

# API integrations (optional — only needed if you enable the relevant plugin)
covid
jikanpy
gtts
speechrecognition
textblob
gingerit
shazamio
speedtest-cli
pokedex.py
quotefancy

# Lyrics / music
git+https://github.com/New-dev0/AsyncLyricsExtractor.git

# Ph logo (random placeholder logo generator)
phlogo

# OCR (optional — only if you use the ocr.py plugin)
# pytesseract
