# AstralBot core dependencies — pinned for reproducibility.
# Plugin-specific dependencies are listed separately in AstralModules/requirements.txt
# and should be installed alongside these.

# MTProto client (Pyrogram v2 fork with active maintenance)
pyrogram==2.0.106
tgcrypto==1.2.5

# Async DB drivers
aiosqlite==0.20.0
motor==3.5.1              # only needed if DATABASE_URL points to MongoDB

# Config / env
python-dotenv==1.0.1

# HTTP client (used by helpers/net.py and many plugins)
aiohttp==3.10.5
requests==2.32.3

# Media / image processing (used by sticker / image plugins)
Pillow==10.4.0

# Web setup wizard (one-command deploy)
flask==3.0.3

# Misc utilities
psutil==6.0.0
pytz==2024.1
